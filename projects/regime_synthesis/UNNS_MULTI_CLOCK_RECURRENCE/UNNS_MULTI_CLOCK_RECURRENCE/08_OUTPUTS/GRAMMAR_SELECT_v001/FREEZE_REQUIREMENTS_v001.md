# FREEZE REQUIREMENTS AFTER GRAMMAR SELECTION

The compact grammar has now been selected.

The freeze stage must not reopen representation selection.

It must commit the following selected items exactly:

1. **Domain**
   - two externally specified incommensurate clocks;
   - genuine time-domain observable;
   - no response-derived clock;
   - v001 empirical support restricted to the golden-ratio branch.

2. **P_src**
   - JPR parent gain;
   - PHASE_LABEL_PERMUTE null;
   - candidate upper-tail probability gate <= 0.10.

3. **J_frac**
   - JPR cover advantage;
   - always reported;
   - no admission threshold.

4. **M_frac**
   - FC fractional mixed gain;
   - candidate raw gate >= 0.12;
   - FOURIER_PHASE upper-tail probability gate <= 0.10;
   - must retain raw gate under the frozen robustness suite.

5. **No composite score.**

6. **C_coll**
   - remains a separate source-native physical evidence interface;
   - no universal numeric gate is to be invented during freeze.

7. **C003 remains excluded until after prospective reveal.**

Freeze may version-bump instead of proceeding if any of these selected choices is reopened.
