# Processed data folder

Canonicalized CSV/Parquet files go here after adapter conversion.

Required minimal columns:

```text
device, shot_id, time
```

Preferred columns follow `schemas/canonical_plasma_timeseries.schema.json`.
