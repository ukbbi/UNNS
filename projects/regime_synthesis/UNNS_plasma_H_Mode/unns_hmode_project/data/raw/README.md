# Raw data folder

No raw experimental data are included in this starter archive.

Place downloaded or exported source files here, grouped by source:

```text
data/raw/
  tcv_zenodo_lh/
  fair_mast/
  mast_paper_tables/
  itpa_hmode/
```

Keep original filenames unchanged. Convert only into `data/processed/`.
