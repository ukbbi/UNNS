JHTDB HIGH-RE SPATIAL GENERALIZATION v0.1

This is a separate preregistered branch. It does not replace or modify Pilot B.

Merge the UNNS_TURB_JHTDB_v0_1 folder from this ZIP into the existing project.
Then use tools\acquire\JHTDB_HIGH_RE_v0_1_0 on SciServer.

The source selection and analysis protocol are frozen before high-Re field values are inspected.
