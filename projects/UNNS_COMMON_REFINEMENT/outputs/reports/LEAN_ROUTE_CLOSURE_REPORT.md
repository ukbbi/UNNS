# Lean Route-Closure Formalization Report

## Status

**LEAN KERNEL VERIFIED**

The narrow **Well-Founded Route-Closure Induction Proposition** has been independently translated into Lean against the same algebraic interface used by the audited Conway refinement repository.

Formalization file:

`04_PROOF_MAP/lean/RouteClosureInduction.lean`

Source lock:

```text
Repository: gaearon/conway-refinement
Commit:     264445c93b78554c408e99e4e7f663693b4e91ab
Lean:       4.31.0
```

## What is formalized

The Lean file defines:

`HasWellFoundedRouteClosure`

and proves:

`forall_isPrimal_of_wellFoundedRouteClosure`

followed by:

`hasFourFactorRefinement_of_wellFoundedRouteClosure`.

Thus the formal dependency chain is exactly:

```text
primal base
+ local routed block
+ strict residual-rank descent
        ↓
every element IsPrimal
        ↓
HasFourFactorRefinement
```

## Reused Conway algebra

The proof deliberately reuses, rather than redefines, the repository's existing results:

- `IsPrimal` from Mathlib divisibility;
- `HasFourFactorRefinement` from `ConwayRefinement.Algebra.Divisibility.Refinement`;
- `exists_primalRefinement_of_factor_refinement` for the splice step;
- `hasFourFactorRefinement_iff_forall_isPrimal` for the final conversion.

This is the formal compatibility test between the UNNS route-closure synthesis and the source algebra.

## Zero/base adaptation

The prose proposition is stated for a cancellative monoid. The Conway generic refinement layer works in a `CommMonoidWithZero` with `IsCancelMulZero`.

The formal version therefore requires `Base 0`. This is not an additional mathematical mechanism; it assigns zero to the terminal/base branch so that the induction uses the same source interface as the Conway algebraic layer.

## Proof structure

For a fixed element `a`, well-founded induction is pulled back along the structural rank map.

- If `a` is in the base regime, primality is immediate.
- Otherwise, for `a ∣ b*c`, local route closure gives
  `a = t*w`, `t = e*f`, `e ∣ b`, `f ∣ c`, and lower rank for `w`.
- The induction hypothesis supplies `IsPrimal w`.
- A witness for `a ∣ b*c` is converted into the product equality required by
  `exists_primalRefinement_of_factor_refinement`.
- The repository splice theorem returns the primal decomposition of `a`.
- Finally `hasFourFactorRefinement_iff_forall_isPrimal` yields four-factor refinement.

## Kernel verification

The module was compiled in a local Windows checkout of the exact locked repository commit.

Build placement:

```text
ConwayRefinement/UNNS/RouteClosureInduction.lean
```

Command:

```text
lake build ConwayRefinement.UNNS.RouteClosureInduction
```

First full target build:

```text
[837/837] Built ConwayRefinement.UNNS.RouteClosureInduction
Build completed successfully (837 jobs).
```

Second logged cached recheck:

```text
[829/829] Replayed ConwayRefinement.UNNS.RouteClosureInduction
Build completed successfully (829 jobs).
```

The informational `linter.hashCommand` notices were generated by diagnostic `#check` commands and were not compilation failures.

## Evidence retained

- `outputs/records/ROUTE_CLOSURE_KERNEL_VERIFICATION.md`
- `outputs/records/ROUTE_CLOSURE_KERNEL_CHECK.txt`
- `outputs/records/ROUTE_CLOSURE_KERNEL_EVIDENCE.png`
- `outputs/records/LEAN_ROUTE_CLOSURE_RESULT.json`

All three kernel-evidence artifacts are now part of the retained project record.

## Scope boundary

This formalization independently verifies the **extracted route-closure induction mechanism**.

It does **not** claim to reimplement from scratch the full concrete Hahn-series / surreal machinery that establishes the routed-block hypothesis in the Conway proof. Those source-specific analytic/order-theoretic ingredients remain represented by the source audit.

Accordingly:

```text
extracted induction principle:
    independently formalized + kernel verified

concrete Hahn/surreal realization:
    source-audited + final theorem independently rebuilt

external specialist mathematical review:
    still open
```

## Project consequence

This kernel verification closes the previously open internal task of formalizing the route-closure induction principle.

For the original Conway/common-refinement investigation, the canonical endpoint should therefore be read together with:

`04_PROOF_MAP/output/FINAL_ROUTE_CLOSURE_SYNTHESIS.md`.

Further STRUC-I / STRUC-PERC-I work is a new structural-phenotype branch, not theorem confirmation.