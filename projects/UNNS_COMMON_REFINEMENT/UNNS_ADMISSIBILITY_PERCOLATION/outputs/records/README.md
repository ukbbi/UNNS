# Records

Machine-readable run records, source locks, hashes, corpus versions and synthesis status JSON files belong here.

Every scientific run should record enough information to reconstruct:

- source corpus version;
- adapter version;
- normalization;
- chamber hashes;
- run timestamp/version;
- output paths.
