# Failures

Exact negative controls only:

- `R1_FAIL` — rank-one non-refinement witnesses, including `H=<2,3>`;
- `AFF_FAIL` — higher-rank positive-affine relation counterexamples.

Only cases with an exact parent-project non-traceability/refinement-failure certificate belong here.
